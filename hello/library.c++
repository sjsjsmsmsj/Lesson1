#include<iostream>
#include<math.h>
using namespace std;
int main() {
    int x = - 100;
    int y = abs(x);
    cout << y << endl;
    int a = 2;
    int b = 10;
    int res = pow(a, b);
    cout << res << endl;
    float to = sqrt(a);
    cout << to << endl;
    return 0;
}